from dataclasses import dataclass
from typing import Any

# Placeholders for Value Specification and Definition
# Actual implementation will come in the next task
@dataclass(frozen=True)
class Specification:
    pass

@dataclass(frozen=True)
class Definition:
    pass
